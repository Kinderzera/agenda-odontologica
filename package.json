{
  "name": "agenda-odontologica",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.5.0"
  },
  "scripts": {
    "start": "node --env-file=.env.local servidor-local.js",
    "dev": "node --env-file=.env.local servidor-local.js"
  },
  "dependencies": {
    "@neondatabase/serverless": "^1.1.0"
  }
}
